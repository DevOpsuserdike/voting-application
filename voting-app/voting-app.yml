apiVersion: v1
kind: Pod
metadata:
  name: voting-app
  labels:
    name: voting-app
spec:
  containers:
  - name: voting-app
    image: siddheshdike/voting-app:1.0.0
    ports:
      - containerPort: 80
